const initialState = {
    data: [],
    loading: false,
    error: null
};

const codeReducer = (state = initialState, actions) => {
    switch (actions.type) {
        default:
            return state;
    }
};

export default codeReducer;